import React from "react";

const AccordionDocs = ({children}) => {
    return (
        <div className="accordion" id="accordionExample">
            {children}
        </div>
    )
}

export default AccordionDocs;